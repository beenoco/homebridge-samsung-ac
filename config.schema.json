{
  "pluginAlias": "BeenocoSamsungAC",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Optional content to display above the plugin config. Supports markdown.",
  "footerDisplay": "Optional content to display below the plugin config. Supports markdown.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "description": "A friendly name for the controlled device.",
        "type": "string",
        "required": true,
        "default": "Wi-Fi Kit",
        "placeholder": "Wi-Fi Kit",
        "typeahead": {
          "source": [
            "Wi-Fi Kit"
          ]
        }
      },
      "deviceModel": {
        "title": "Wi-Fi Kit Model",
        "description": "The model of the controlled device.",
        "type": "string",
        "required": false,
        "placeholder": "MIM-H03N",
        "typeahead": {
          "source": [
            "MIM-H03",
            "MIM-H03N",
            "MIM-H04"
          ]
        }
      },
      "deviceMACAddress": {
        "title": "MAC Address",
        "type": "string",
        "required": false,
        "placeholder": "aa:bb:cc:dd:ee:ff",
        "pattern": "^([A-Fa-f0-9]{2}:){5}[A-Fa-f0-9]{2}$",
        "description": "The unique MAC address of the Wi-Fi Kit. May be obtained from your router."
      },
      "deviceIPAddress": {
        "title": "IP Address",
        "type": "string",
        "required": true,
        "format": "ipv4",
        "placeholder": "192.168.8.1",
        "description": "The IP address of the Wi-Fi Kit. Must be statically assigned in your router configuration."
      },
      "deviceToken": {
        "title": "Device Token",
        "type": "string",
        "required": true,
        "placeholder": "xxxxxxxxxx",
        "description": "The device token retrieved from the Wi-Fi Kit during pairing"
      },
      "devicePollInterval": {
        "title": "Poll Interval",
        "type": "integer",
        "required": true,
        "minimum": 1,
        "maximum": 60,
        "default": 15,
        "placeholder": 15,
        "description": "In seconds. Poll for device state changes at this interval."
      },
      "devices": {
        "title": "Controlled Devices",
        "type": "array",
        "items": {
          "title": "Device",
          "type": "object",
          "properties": {
            "name": {
              "title": "Name",
              "type": "string",
              "required": true,
              "default": "Air Conditioner",
              "placeholder": "Air Conditioner",
              "typeahead": {
                "source": [
                  "Air Conditioner"
                ]
              }
            },
            "id" : {
              "title": "Device ID",
              "type": "string",
              "required": true,
              "placeholder": "000000000",
              "description": "The ID of the controlled device."
            }
          }
        },
        "description": "All air conditioning devices controlled by this Wi-Fi Kit."
      }
    }
  }
}